$('div.post[data-id="<%= @post.id %>"] ul.like').replaceWith('<%= escape_javascript(like_link @post) %>');
$('div.post[data-id="<%= @post.id %>"] ul.comment div.like_people').parent().remove();
$('div.post[data-id="<%= @post.id %>"] ul.comment').prepend('<%= escape_javascript(like_people @post) %>');