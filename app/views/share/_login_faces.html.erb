<% if @users.present? %>
<aside class="faces">
  <ul>
    <% @users.each do |user| %>
    <li><%=link_to image_tag(user.image), users_home_path(user.id) %></li>
    <% end %>
  </ul>
</aside>
<% end %>