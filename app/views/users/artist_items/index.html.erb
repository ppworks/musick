<ul class="items pagination_content">
<% @artist_items.each do |artist_item| %>
  <li><%= render 'artist_item', :artist_item => artist_item %></li>
<% end %>
</ul>
<div id="paginator">
  <%= paginate @artist_items, :remote => true %>
</div>
