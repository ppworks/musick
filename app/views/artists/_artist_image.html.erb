<section class="artist_image clearfix" style="width: <%=size[:width]%>px;height: <%=size[:height]%>px;">
  <div class="image">
  <%= link_to_artist_image image_tag(artist_image.original, {:size => "#{size[:width]}x#{size[:height]}"}), artist_image %>
  </div>
</section>
